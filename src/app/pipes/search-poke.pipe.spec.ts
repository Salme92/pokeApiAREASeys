import { SearchPokePipe } from './search-poke.pipe';

describe('SearchPokePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPokePipe();
    expect(pipe).toBeTruthy();
  });
});
