import { SearchPokeTypePipe } from './search-poke-type.pipe';

describe('SearchPokeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPokeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
